apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: birdeye-addons
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - name: grafana
            path: addons/grafana
            valueFile: clusters/Birdeye-eks-cluster/addons/grafana.yaml
            namespace: monitoring

          - name: ingress-nginx
            path: addons/ingress-nginx
            valueFile: clusters/Birdeye-eks-cluster/addons/ingress-nginx.yaml
            namespace: ingress-nginx

          - name: keda
            path: addons/keda
            valueFile: clusters/Birdeye-eks-cluster/addons/keda.yaml
            namespace: keda

          - name: loki
            path: addons/loki
            valueFile: clusters/Birdeye-eks-cluster/addons/loki.yaml
            namespace: loki

          - name: prometheus
            path: addons/prometheus
            valueFile: clusters/Birdeye-eks-cluster/addons/prometheus.yaml
            namespace: monitoring

          - name: statsd-exporter
            path: addons/statsd-exporter
            valueFile: clusters/Birdeye-eks-cluster/addons/statsd-exporter.yaml
            namespace: monitoring

          - name: tempo
            path: addons/tempo
            valueFile: clusters/Birdeye-eks-cluster/addons/tempo.yaml
            namespace: tempo

          - name: aws-load-balancer-controller
            path: addons/aws-alb-controller
            valueFile: clusters/Birdeye-eks-cluster/addons/aws-alb-controller.yaml
            namespace: kube-system

  template:
    metadata:
      name: '{{name}}'
      namespace: argocd
    spec:
      project: default
      source:
        repoURL: https://github.com/devopsraj178-ops/eks-addons.git
        targetRevision: main
        path: '{{path}}'
        helm:
          valueFiles:
            - '../../{{valueFile}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
